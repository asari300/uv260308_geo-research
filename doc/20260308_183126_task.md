# タスクリスト

- [ ] 市町村モードの地点名からURIや `[` `]` などのマークダウンのカッコを非表示に修正する
- [ ] data_generator.py を修正し、最高だけでなく最低の指標も正しく生成しStreamlitからも選択できるようにする
- [ ] data_generator.py であらかじめ指標ごとの上下5位を計算し、それぞれ個別の `.tsv` ファイルとして出力する
- [ ] app.py を修正し、生成された `.tsv` を読み込んで高速にデフォルト表示（上下5位）をするように対応する
- [ ] Playwright MCP のセットアップとテスト手順書を `.md` にまとめる
- [ ] 今回追加・更新した Task, implementation plan, walkthrough などをすべて `doc/` 配下へタイムスタンプ付きで作成する
